<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Hajimi24" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <!-- 强制使用我们内置的字体族 -->
        <item name="android:fontFamily">@font/app_default_font</item>
        <item name="fontFamily">@font/app_default_font</item>

        <!-- 禁用系统字体缩放（可选，防止用户调大字体导致 UI 错位） -->
        <item name="android:textSize">16sp</item>
    </style>

    <style name="Theme.Hajimi24" parent="Base.Theme.Hajimi24" />
</resources>